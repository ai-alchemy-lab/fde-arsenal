# 贡献规格

> 这份文档是写给 AI 读的。
>
> 如果你是一家公司、一个团队，想把自己在某一站上的实践挂进这个库：把这份文档连同你们自己的项目材料一起丢给你们的 AI，让它按下面的规格产出一个 md 文件，然后提 PR。格式的事交给它，我审的是内容。

## 一、这个库的两层结构

| 层 | 目录 | 谁写 | 能不能改 |
|---|---|---|---|
| 骨架 | `book/`、`02-方法论/`、`03-模板库/`、`04-案例库/` | 任鑫 | **不接受外部改动**。有异议提 issue 讨论 |
| 实践墙 | `08-实践墙/` | 所有人 | 这是你要提交的地方 |

骨架是判断，实践墙是肉。骨架不动，肉越填越厚。

## 二、挂载点（必填，不要自创）

一个文件只能挂一个挂载点，值必须是下面之一：

```
S1-画像与筛选    S2-安排与授权    S3-选题
S4-真问题与Context    S5-Eval    S6-造与测
S7-上线与采纳    S8-算账    S9-带第二个人
T-模板改进    X-行业包
```

各挂载点收什么，见 [08-实践墙/README](08-实践墙/README.md)。

文件路径就是 `08-实践墙/<挂载点>/<贡献方>-<一句话主题>.md`。

## 三、frontmatter 规格

每个文件必须以这段 YAML 开头，字段一个都不能少：

```yaml
---
挂载点: S4-真问题与Context      # 上面那张表里的值，不要自创
贡献方: 毕昇（数据项素）           # 公司或团队名，会显示在页面顶部
类型: 现场做法                    # 现场做法 | 失败复盘 | 行业变体 | 模板改进
行业: 保险                        # 具体行业，不写"多行业"
企业规模: 千人级                  # 百人级 | 千人级 | 万人级
时间: 2025 年下半年                # 这件事发生的时间，不是你写文档的时间
可核验: 部分                      # 全部公开 | 部分 | 内部数据已脱敏
联系: https://...                 # 一行链接，放在页面底部
---
```

`可核验` 这一栏是必填且不许美化的。写「内部数据已脱敏」不丢人，写「全部公开」但查不到才丢人。

## 四、正文骨架

**类型是「现场做法」或「失败复盘」时**，用这六段，标题原样保留：

```markdown
## 背景
一句话说清：什么行业、多大规模、什么场景、当时要解决什么。不超过 150 字。

## 当时卡在哪
具体的卡点。骨架里哪一节讲过这个卡点，写清楚（例如「对应第 6 章第八节，数据看门人」）。

## 我们实际怎么做的
动作，不是理念。带话术、带表格、带顺序。这一段是整篇的主体，越具体越好。

## 结果
带分母的数字。没有分母的百分比会被退回。
失败也写在这一栏，失败比成功稀缺。

## 和骨架不一样的地方
你们的做法和书里写的哪里不同，为什么。这一栏是这份文档最值钱的地方——
如果完全一样，这条贡献没有必要存在。

## 别人抄的时候要注意什么
什么前提下这套做法不成立。这一栏空着的稿子不会被合并。
```

**类型是「模板改进」时**，用这四段：`## 改的是哪件模板` / `## 原版哪里不好使` / `## 我们改成了什么` / `## 改完之后的效果`。

**类型是「行业变体」时**，用一张表把九站过一遍，每站一行，写这一行在你们行业的特殊之处；没有特殊之处的站写「同骨架」。

## 五、硬性要求

**必须有分母。** 「效率提升 300%」会被退回。「单笔复核从 6 分钟降到 1 分钟，全年 1.4 万单」会被合并。

**产品名不能出现在解法位置。** 可以写你们是谁、可以在底部留链接、可以说清楚这是贵司做的。但方法本身要写成任何人都能抄的形态。一段只有买了你产品才能执行的方法，对读者没有价值，对你也没有说服力。

**过关标准是别人的行为。** 这是这本书对自己的标准，对贡献一视同仁。「客户很满意」不算数；「上线第三周有 11 个人连着五天自己打开了它，其中 3 个原来是反对的」算数。

**想提一个成功案例，先提一个失败复盘。** 这一条是硬规矩。理由有两个：失败素材在中文世界几乎不存在，这是这个库最稀缺的部分；以及，真做过的人提得出来。

**不许编数字。** 记不清就写量级并注明。被发现编数字，该贡献方所有条目全部下线。

## 六、不收什么

课程与服务广告（包括伪装成经验分享的软文）。没有一线细节的成功故事。未经真实交付验证的方法论综述。任何成功学与焦虑营销。

裁判标准只有一条：**这个内容能不能帮到明天早上要进现场的那个人**。

## 七、文风约定

中文写作，口语化，具体数字，来源可查。

禁用词：赋能、颠覆、抓手、闭环（作为空词使用时）、生态、全链路、一站式。

不要写「我们通过 xx 实现了 yy」这种句式，写「我们干了什么，然后发生了什么」。

## 八、提交流程

1. Fork 本仓库
2. 在 `08-实践墙/<挂载点>/` 下新建文件
3. 按上面的规格填好 frontmatter 和正文骨架
4. 提 PR，标题格式：`[实践墙] <挂载点> · <贡献方> · <一句话主题>`

审核只看内容，不看格式——格式请你的 AI 按这份文档处理好。

## 九、其他两类贡献

**情报线索**：新的重要文章、数据、玩家动向，提 issue 给 [07-情报站](07-情报站/)。

**海外材料中文导读**：认领后参照 [01-原典](01-原典/) 的格式。导读不是翻译——用自己的话、按原文结构提炼、明显短于原文、附原文链接。
